# MCP Server Setup

This workspace now includes a simple MCP server entry point at `mcp-server.js`.

## Run

```bash
node mcp-server.js
```

## Tool exposed

- `list_workspace_files`
  - Optional input: `folder`

This can be used by MCP-compatible clients over stdio.
